#!/bin/sh

if [ "${1:0:7}" == "http://" ] || [ "${1:0:8}" == "https://" ];then
   args="-a 'Google Chrome.app' $args"
fi

while [ "$1" ];do
    args="$args '$1'"
    shift
done
host-exec "open $args"
